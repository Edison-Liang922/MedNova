% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mednova_task_assistant.R
\name{mednova_task_assistant}
\alias{mednova_task_assistant}
\title{规划医学研究任务并生成 R 脚本}
\usage{
mednova_task_assistant(
  data,
  task,
  spec = list(),
  data_name = "input_data",
  output = c("list", "script"),
  save_to = NULL
)
}
\arguments{
\item{data}{用户提供的数据对象，或者一个字符标量，用于指定脚本中的对象名。}

\item{task}{自然语言任务描述。}

\item{spec}{可选的命名 list，用于提供任务相关提示，例如 \code{outcome}、
\code{treatment}、\code{covariates}、\code{time}、\code{event}、\code{group}、
\code{case_level}、\code{control_level}、\code{col_data_name}、
\code{exposure_data_name} 或 \code{outcome_data_name}。}

\item{data_name}{脚本中使用的数据对象名。优先级依次为：显式 \code{data_name}，
然后是 \code{spec$data_name}，最后回退到 \code{"input_data"}。}

\item{output}{输出模式。\code{"list"} 返回结构化结果；\code{"script"} 只返回脚本字符串。}

\item{save_to}{可选文件路径，用于保存生成的脚本。}
}
\value{
当 \code{output = "list"} 时，返回包含 \code{data_profile}、
\code{matched_domain}、\code{selected_functions}、\code{workflow_steps}、
\code{assumptions} 与 \code{script} 的 list；当 \code{output = "script"} 时，
返回单个脚本字符串。
}
\description{
MedNova 不会自动执行分析。这个函数只负责检查输入对象、匹配任务域、
推荐函数，并返回可直接运行的 R 脚本文本。
}
