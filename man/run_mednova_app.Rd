% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_mednova_app.R
\name{run_mednova_app}
\alias{run_mednova_app}
\title{启动 MedNova Studio}
\usage{
run_mednova_app(
  launch.browser = interactive(),
  host = "127.0.0.1",
  port = NULL,
  display.mode = c("auto", "normal", "showcase"),
  return_app = FALSE
)
}
\arguments{
\item{launch.browser}{是否在浏览器中打开应用。}

\item{host}{传递给 \code{\link[shiny:runApp]{shiny::runApp()}} 的 host。}

\item{port}{传递给 \code{\link[shiny:runApp]{shiny::runApp()}} 的 port。为 \code{NULL} 时由 Shiny 自行处理。}

\item{display.mode}{传递给 \code{\link[shiny:runApp]{shiny::runApp()}} 的显示模式。}

\item{return_app}{逻辑值；当 \code{TRUE} 时返回 Shiny app 对象而不直接启动。
这个参数主要用于 smoke test。}
}
\value{
不显式返回时会启动应用；当 \code{return_app = TRUE} 时返回
一个 \code{shiny.appobj}。
}
\description{
启动 MedNova Studio 工作台，用于配置任务输入并生成可执行的 R 脚本。
}
